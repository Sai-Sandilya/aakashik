{
  "name": "aakashik-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch src/server.js",
    "test": "NODE_ENV=test node --test tests/**/*.test.js",
    "test:verbose": "NODE_ENV=test node --test --test-reporter=spec tests/**/*.test.js"
  },
  "dependencies": {
    "@fastify/cookie": "^11.1.2",
    "@fastify/cors": "^11.0.1",
    "better-sqlite3": "^11.10.0",
    "fastify": "^5.3.2",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^9.0.6"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
